#include <iostream>
#include <unordered_map>

vector<int> allPrimesUpto(int num) {
  /*
   * Sieve of Eratosthenes
   *
   * */
  vector<bool> marked;

  for(int n = 0; n <= num; ++n) {
    marked[n] = false;
  }

  for(int n = 2; n <= num; ++n) {
    if ( unmarked.find(n) != unmarked.end() ) {
      for(
    }
  }



}
